#include "main.h"
#include <stdio.h>

/**
 * main - Print the name of the file
 * @argc: argument count
 * @argv: arguments array
 * Return: zero
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);

	return (0);
}
